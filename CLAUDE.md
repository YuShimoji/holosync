# HoloSync AI Adapter

This file is kept only for tool compatibility.

## Read First

1. `docs/AI_RULES.md`
2. `docs/runtime-state.md`
3. Relevant spec under `docs/specs/` only when the task touches that feature

## Boundary

- Do not treat this file as project state.
- Do not add handoff logs, feature ledgers, or decision history here.
- Current project state belongs in `docs/runtime-state.md`.
- Feature specifications belong in `docs/specs/` and are indexed by `docs/spec-index.json`.
