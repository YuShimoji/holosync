# Minimal Codex project config scaffold.
# Keep this file short and project-specific.
# Canonical behavioral rules live in docs/ai/*.md and AGENTS.md.

model = "gpt-5-codex"
approval_policy = "on-request"
sandbox_mode = "workspace-write"

# Add project-specific hooks or tool restrictions here if needed.
